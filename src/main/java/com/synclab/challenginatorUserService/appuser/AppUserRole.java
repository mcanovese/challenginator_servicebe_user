package com.synclab.challenginatorUserService.appuser;


//Enum definizione ruoli utente per controlli accesso via Security - di default tutti sono USER
public enum AppUserRole {

    USER,
    ADMIN
}
